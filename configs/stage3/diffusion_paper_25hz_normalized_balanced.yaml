schema_version: stage3-diffusion-config-v1
frequency_hz: 25.0
dt: 0.04
past_steps: 4
include_current: true
future_steps: 16
sequence_length: 21
future_physical_horizon_s: 0.64
prediction_type: x0
model:
  embedding_dim: 512
  attention_heads: 8
  transformer_layers: 6
diffusion:
  denoising_steps: 20
  per_token_steps: true
normalization:
  enabled: true
loss:
  reduction: group_mean
  state_weight: 1.0
  latent_weight: 1.0
training:
  batch_size: 512
  epochs: 1000
  learning_rate: 0.0001
  weight_decay: 0.001
  scheduler: cosine
  warmup_steps: 10000
  mixed_precision: fp32
  gradient_accumulation_steps: 1
ema:
  power: 0.75
  max: 0.9999
